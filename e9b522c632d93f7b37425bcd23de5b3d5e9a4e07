This is done by a Jenkins job "auto-merge"

This is done by a Jenkins job "auto-merge"

This is done by a Jenkins job "auto-merge"

This is done by a Jenkins job "auto-merge"

This is done by a Jenkins job "auto-merge"
